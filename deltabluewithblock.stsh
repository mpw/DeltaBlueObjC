#!/usr/local/bin/stsh
framework:DeltaBlue load.
a := 2.
b := 5.

solver := DBSolver solver.
constraint := solver constraintWithSTBlock:[ b:= a * 20].

constraint add1ArgBlock:[ :arg | arg / 20 ]. 
shell runInteractiveLoop.
