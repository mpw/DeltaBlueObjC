#!/usr/local/bin/stsh
framework:DeltaBlue load.
a := 2.
b := 5.

solver := DBSolver solver.
constraint := solver constraintWithSTBlock:[ a:= b * 20].

constraint add1ArgBlock:[ :a | a / 20 ]. 
shell runInteractiveLoop.
